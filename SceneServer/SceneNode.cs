using Common.Net;

namespace SceneServer;

public class SceneNode : NetNode
{
    public SceneNode(EServiceType serviceType) : base(serviceType)
    {
    }
    
    public override void onReceive(ConnectionEventArgs args)
    {
        if(args.)
    }
}